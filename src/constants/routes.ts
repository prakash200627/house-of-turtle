/**
 * Route Constants
 * Provides centralized, type-safe references to page paths.
 */
export const ROUTES = {
  HOME: "/",
  SHOP: "/shop",
  COLLECTIONS: "/collections",
  CART: "/cart",
  ABOUT: "/about",
} as const;
